using CSVQueryLanguage.Analysis;
using CSVQueryLanguage.Parser.Tree;

namespace CSVQueryLanguage.Plan;

public sealed class ExpressionRewriter
{
    public IExpression Rewrite(IExpression expression, QueryScope scope)
    {
    }
}
